# Verification and Validation

This folder contains a validation script and the required data to run it to validate the Python implementation using results obtained from the MOEA benchmark Java implementation. The MOEA data was obtained using the NSGAII algorithm run for 10000 generations.
A basic validation report is generated by the validation script and contained in the "validation_report.txt" file.
